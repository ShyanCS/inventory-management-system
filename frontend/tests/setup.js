/**
 * Vitest test setup file.
 *
 * Configures @testing-library/jest-dom matchers (e.g., toBeInTheDocument)
 * and will set up the MSW mock server in later phases.
 */
import '@testing-library/jest-dom'
